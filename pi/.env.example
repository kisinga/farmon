# Copy to .env and edit. Used by docker-compose.
# On Pi after setup_gateway.sh: uncomment and use these so backend sees the gateway and receives uplinks.

CONCENTRATORD_EVENT_URL=ipc:///tmp/concentratord_event
CONCENTRATORD_COMMAND_URL=ipc:///tmp/concentratord_command
CONCENTRATORD_GATEWAY_ID=local
