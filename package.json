{
  "name": "waterctl",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app && npm run build:electron --prefix app",
    "test": "npx --prefix app tsx app/test/codegen.test.ts",
    "test:limits": "npx --prefix app tsx app/test/limits.test.ts"
  }
}
